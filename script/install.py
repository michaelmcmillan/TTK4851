from sys import version_info

# python version check
if not version_info.major == 2 and not version_info.minor == 7:
    raise Exception('Prosjektet krever at du har Python 2.7 installert.')

# pip version check

# check if virtualenv is installed

# check if env exists

# create env if does not exist

# activate env and install lego-nxt + write to requirements
